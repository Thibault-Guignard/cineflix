{% extends 'back/user/user_template.html.twig' %}


{% block bodyBack %}
    <h1>Utilisateur : {{ user.email }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_user_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('back_user_edit', {'id': user.id}) }}" class="btn btn-warning">edit</a>

    {{ include('back/user/_delete_form.html.twig') }}
{% endblock %}
