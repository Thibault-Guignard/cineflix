{% extends 'back/user/user_template.html.twig' %}

{% block body %}
    <h1>Ajouter un utilisateur</h1>

    {{ include('back/user/_form.html.twig') }}

    <a href="{{ path('back_user_index') }}" class="btn btn-primary">Retour à la liste</a>
{% endblock %}
