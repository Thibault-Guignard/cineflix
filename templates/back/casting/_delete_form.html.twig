<form method="post" action="{{ path('back_casting_delete', {'id': casting.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ce film/cette série ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ casting.id) }}">
    <button class="btn btn-danger mt-2">Delete</button>
</form>
