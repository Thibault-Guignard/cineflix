{% extends 'back/casting/casting_template.html.twig' %}

{% block body_back %}
    <h2>Modifer le casting </h2>

    {{ include('back/casting/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

    {{ include('back/casting/_delete_form.html.twig') }}

    <a href="{{ path('back_casting_index',{id: casting.movie.id}) }}" class="btn btn-primary">Retour a liste</a>
{% endblock %}
