{% extends 'back/movie/movie_template.html.twig' %}

{% block bodyBack %}
    <h2>Modifer le </h2>

    {{ include('back/movie/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

    {{ include('back/movie/_delete_form.html.twig') }}
    
    <a href="{{ path('back_movie_index') }}" class="btn btn-primary">Retour a liste des films</a>

{% endblock %}
